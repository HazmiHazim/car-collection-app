import { FC } from "react";
import MainLayout from "../layout/mainLayout";

const Colour: FC = () => {
  return (
    <MainLayout>
      <div>This is Colour page</div>
    </MainLayout>
  );
};

export default Colour;
